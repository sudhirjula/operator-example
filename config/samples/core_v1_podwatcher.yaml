apiVersion: core.example.com/v1
kind: PodWatcher
metadata:
  labels:
    app.kubernetes.io/name: operator-example
    app.kubernetes.io/managed-by: kustomize
  name: podwatcher-sample
spec:
  # TODO(user): Add fields here
